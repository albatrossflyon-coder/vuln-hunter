fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
strawberry-graphql>=0.235.0
python-dotenv>=1.0.0
openai>=1.50.0
semgrep>=1.90.0
mcp>=1.28.1,<2
pip-audit>=2.7.0
cryptography>=50.0.0
click>=8.3.3
protobuf>=5.29.6,<7
psutil>=6.0.0
textual>=0.53.0
rich>=13.7.0

# dev/test only
jsonschema>=4.25.0

# post-scan triage: turns results.sarif into business-prioritized findings
# (Threat x Reachability x Consequence), not just severity. `vulnometry import
# results.sarif` after a scan. Evaluated + scanned clean 2026-09-13.
vulnometry>=0.3.1
